apiVersion: apps/v1
kind: Deployment
metadata:
  name: predictionservice
  namespace: smartlend
  labels:
    app: predictionservice
spec:
  replicas: 1
  selector:
    matchLabels:
      app: predictionservice
  template:
    metadata:
      labels:
        app: predictionservice
    spec:
      containers:
        - name: predictionservice
          image: smartlend/predictionservice:dev
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8007
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: k8s
            - name: JAVA_OPTS
              value: "-Xms256m -Xmx512m"
          envFrom:
            - configMapRef:
                name: smartlend-config
            - secretRef:
                name: smartlend-secrets
          resources:
            requests:
              memory: 384Mi
              cpu: 200m
            limits:
              memory: 768Mi
              cpu: 1000m
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8007
            initialDelaySeconds: 90
            periodSeconds: 15
            timeoutSeconds: 10
            failureThreshold: 6
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8007
            initialDelaySeconds: 150
            periodSeconds: 30
            timeoutSeconds: 10
            failureThreshold: 5
---
apiVersion: v1
kind: Service
metadata:
  name: predictionservice
  namespace: smartlend
spec:
  selector:
    app: predictionservice
  ports:
    - port: 8007
      targetPort: 8007
