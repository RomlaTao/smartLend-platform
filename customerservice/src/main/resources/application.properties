spring.application.name=customerservice

server.port=8007

jwt.secret=2L7OaWJq8w3sM7vM8q5Gq0Wz6JfYzI6aQyd0NgqfFs8=
jwt.expiration=86400000

spring.datasource.url = ${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3309/customerservice?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC&createDatabaseIfNotExist=true}
spring.datasource.username = ${SPRING_DATASOURCE_USERNAME:customerservice}
spring.datasource.password = ${SPRING_DATASOURCE_PASSWORD:customerservice}
spring.datasource.driver-class-name = com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto = ${SPRING_JPA_HIBERNATE_DDL_AUTO:update}
spring.jpa.show-sql = ${SPRING_JPA_SHOW_SQL:false}
spring.jpa.properties.hibernate.dialect = ${SPRING_JPA_PROPERTIES_HIBERNATE_DIALECT:org.hibernate.dialect.MySQL8Dialect}

eureka.client.service-url.defaultZone = ${EUREKA_SERVER_URL:http://localhost:8761/eureka/}
eureka.instance.prefer-ip-address = true

spring.security.user.name=admin
spring.security.user.password=admin

# ===========================================
# RabbitMQ Configuration
# ===========================================
spring.rabbitmq.host = ${SPRING_RABBITMQ_HOST:localhost}
spring.rabbitmq.port = ${SPRING_RABBITMQ_PORT:5672}
spring.rabbitmq.username = ${SPRING_RABBITMQ_USERNAME:guest}
spring.rabbitmq.password = ${SPRING_RABBITMQ_PASSWORD:guest}

# RabbitMQ Queue Configuration
rabbitmq.exchange.customer-profile-requested = ${RABBITMQ_EXCHANGE_CUSTOMER_PROFILE_REQUESTED:customer.profile.exchange}
rabbitmq.queue.customer-profile-requested = ${RABBITMQ_QUEUE_CUSTOMER_PROFILE_REQUESTED:customer.profile.request}
rabbitmq.routing-key.customer-profile-requested = ${RABBITMQ_ROUTING_KEY_CUSTOMER_PROFILE_REQUESTED:customer.profile.requested}
# ===========================================
# Management & Monitoring
# ===========================================   
# Actuator endpoints
management.endpoints.web.exposure.include=health,info,metrics
management.endpoint.health.show-details=always
management.endpoint.health.show-components=always

# Health check configuration
management.health.db.enabled=true
management.health.redis.enabled=false
